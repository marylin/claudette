# v1.1 Remaining Work — Progress

## Current State
All implementation tasks complete. Build passes (tsc + vite). Pending smoke test.

## Next Steps
Phase F — Task 20: Smoke test

## Tasks

### Phase A — Project Persistence
- [x] 1. Create `src/main/config-manager.ts` — stores to `~/.claudette/config.json`
- [x] 2. Wire project IPC handlers — `project:get-current`, `project:get-recent`, `project:set-current`
- [x] 3. Restore last project on app launch — in `index.ts` ready-to-show
- [x] 4. Project-change lifecycle — watcher + PTY reinit on project switch
- [x] 5. Preload API for project channels — `getCurrentProject`, `getRecentProjects`, etc.

### Phase B — File System Watcher
- [x] 6. Add chokidar dependency — installed chokidar@3
- [x] 7. Create `src/main/fs-watcher.ts` — watches project dir, debounced events
- [x] 8. Wire watcher to project changes — starts in `project:set-current` + on app launch
- [x] 9. Preload listener for `fs:tree-updated` — `onTreeUpdated` method

### Phase C — Git Auto-Refresh
- [x] 10. Git status auto-refresh — watcher emits `git:status-updated` (300ms debounce)
- [x] 11. Preload listener for `git:status-updated` — `onGitStatusUpdated` method

### Phase D — Terminal PTY
- [x] 12. Add node-pty dependency — installed
- [x] 13. Create `src/main/pty-manager.ts` — spawns PowerShell/bash, IPC data forwarding
- [x] 14. Wire terminal IPC handlers — `terminal:start`, `terminal:input`, `terminal:resize`, `terminal:kill`
- [x] 15. Preload API for terminal — `startTerminal`, `sendTerminalInput`, `onTerminalData`, etc.
- [x] 16. Update TerminalPanel.tsx — connected xterm.js to real PTY, restart button on exit
- [x] 17. PTY lifecycle on project change — disposed + re-spawned via `setPtyProject`

### Phase E — CLAUDE.md Enhancements
- [x] 18. Global CLAUDE.md scope — `claude-md:read/write` supports `scope='global'`
- [x] 19. Watch CLAUDE.md for external changes — `claude-md:watch` + `claude-md:changed` events

### Phase F — Smoke Test
- [ ] 20. Build verification + success criteria
